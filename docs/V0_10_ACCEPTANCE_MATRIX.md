# DeepLaw v0.10 acceptance matrix

Status: **release contract**, 2026-07-30.

DeepLaw 2.0 is the product brand. The software version governed here is `v0.10.0`.

Machine decisions:

```text
commercial_release_eligible=true
quality_protocol_eligible=true
competitive_claim_eligible=false
```

The first field is retained for compatibility with release-manifest v1/v2 consumers. The second is
the v0.10 self-verifying quality decision. The third prevents a core quality result from being
rewritten as a comparison that was never run.

## Acceptance

| Requirement | Executable evidence | Pass rule |
| --- | --- | --- |
| Version identity | package, Python runtime, Claude marketplace/plugins, Codex plugins, OpenCode manifest | every value is exactly `0.10.0` |
| Evaluation freeze | protocol freeze-path Git history | candidate is a clean strict descendant of the freeze commit |
| Public benchmark | Evaluation Protocol v1 schemas and four suites | schemas valid; fixed paths and bytes load safely |
| Repository development | lexical/dense/hybrid component report | score `>=0.75`, suite gate passes, zero forbidden admissions |
| Temporal holdout | version/authority-sensitive component report | score `>=0.80`, suite gate passes, zero forbidden admissions |
| Autonomous safety | 12 actual domain-service cases | score `1.00`; zero mutation/elevation/injection/disclosure hard failures |
| Typed Compiler | source-bound bilingual deterministic-v2 gold | score `1.00`; zero hallucinated/unsupported claims |
| Overall quality | fixed weighted score | `>=0.85`, all components pass, `hard_failures=[]` |
| Candidate artifact | isolated exact wheel install and installed-package inventory | report version/commit/tree/wheel SHA match; every loaded package file matches wheel bytes, with no unbound extras |
| Report integrity | verifier and checksum inventory | summary, all component digests, report bytes, and every checksum match |
| Three operating systems | Linux, macOS, Windows mandatory suites | zero failures, errors, and skips; at least 1,740 aggregate tests |
| Distribution lifecycle | exact wheel/sdist plus historical v0.6 fixture | clean install, migration, upgrade, rollback point, snapshot/restore, uninstall |
| Host adapters | Codex, Claude Code, OpenCode no-model acceptance | all three lifecycle checks pass; no model/API result is claimed |
| Supply chain | reproducible build, OCI, audits, SBOM, licenses, OpenVEX | byte-identical wheel/sdist; non-root networkless OCI; all audits pass |
| Release provenance | root checksums, Sigstore OIDC, GitHub attestations | every published manifest artifact is hash-bound and attested |
| Documentation | bilingual README, protocol, traceability, upgrade, security, notes | brand/version and claim boundaries are consistent |

## Quality result interpretation

The formal report is generated by CI from the exact release wheel. Its expected deterministic
functional scores from the frozen suites are:

| Component | Score |
| --- | ---: |
| repository development | `0.777777777778` |
| repository temporal holdout | `0.800000000000` |
| autonomy safety | `1.000000000000` |
| Typed Compiler quality | `1.000000000000` |
| weighted overall | `0.907777777778` |

The release workflow must compute these results again. This document is not a substitute for the
published `evaluation-report.json`; a mismatch or hard failure blocks publication.

## Deliberately unresolved comparative evidence

- Real model-task receipts for Codex, Claude Code, and OpenCode.
- Actual v0.10 runs for every preregistered named comparison system.
- Paired confidence intervals over the comparative per-case results.
- Complete comparative build/query cost and failure inventory.

No external institution certification, secret holdout, or organization signature is required for
the core quality result. The public holdout is maintainer-visible and must never be described as
secret, unseen, or contamination-free.

## Compatibility and rollback

The v0.7 source-derived proposal/review path remains only for source compilation, untrusted import,
migration, and rollback compatibility. It is not the default path for admitted Agent-derived
knowledge. v0.10 preserves the v0.9 autonomous schema and uses the documented snapshot/restore
workflow for rollback; code deletion must not remove historical database migration or integrity
checks.
