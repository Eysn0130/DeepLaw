# DeepLaw v0.13 Pass 7 root-cause ledger

Status: current correctness and qualification-measurement repair only. This is not Human Gold,
real-Host, Legal Pack, Platform Core, artifact-redownload or release evidence.

Baseline: branch `codex/v013-evidence-provenance`, start commit/tree
`09e1c95991329b9dcae41ba1d4dc2d0e215b55ac` /
`dd09017f9ba688592f041bb30914bab4effc7ec2`. Package version remains `0.12.0`.
The accepted implementation commit/tree and current-head CI are recorded in the Pass 7 handoff;
this file does not self-certify its own future Git identity.

| ID | Fail-before reproduction | Root cause | Public surface and risk | Minimum repair | Regression / requalification status |
|---|---|---|---|---|---|
| P7-MOUNT-001 | `validate_product_outcome_package` accepted both (a) a `compiler_evaluator` candidate mount and `evaluator_only` Gold mount resolving to one directory and (b) either direction of parent/child nesting | The v1 topology check compared only `compiler_only` against `evaluator_only`, and compared root equality without closing ancestor/descendant visibility | Owner outcome package; a compiler-visible root could contain Gold/scorer/control/output bytes while the manifest appeared structurally valid | Treat `compiler_only` and `compiler_evaluator` as compiler-visible; treat `evaluator_only` and `owner_evaluator` as protected; resolve every path and reject equality or nesting in either direction. Preserve protected evaluator-domain sharing and explicitly disclaim OS-sandbox proof | Negative tests cover same-root, parent-child, child-parent, symlink resolution, duplicate mounts, incomplete/extra mappings and a legal disjoint package. External OS/container isolation remains `not_executed` |
| P7-QUERY-001 | Public Source → `SemanticCompilationService` → Python/CLI/MCP Context returned `claim:unseen-query-v6:alpha_multilingual` for `Policy Alpha` but dropped it for `policy alpha`; the casing-derived inferred-anchor pool changed the relevance floor | Pass 6 had removed direct `identity_anchor_mismatch`, but final selection still conditionally enabled a hard relevance rule based on whether inferred Titlecase/uppercase hints matched any candidate | Default Query Plan v6 Context; formatting could suppress a required Statement, contradiction, exception or temporal duty. Authority/scope were not elevated, but task correctness changed | Natural-language anchor hints now affect ordering only. Eligibility uses a case-folded content floor plus governed title/alias lexical terms, independent of inferred casing. Explicit `semantic_key`/`knowledge_id`/`revision_id` remain strict. No dictionary, Gold phrase, new threshold stack or wider budget was added | Human-labelled required/optional identities are explicit in the development metamorphic suite. Lower/sentence/upper case, punctuation, quotes, CJK + English, multi-target, ambiguous alias, opaque unknown identity and explicit identity targets pass across Python/CLI/MCP. The corpus is tuning-used development evidence, not Human Gold |
| P7-GRAPH-001 | With four admitted Relations, test scan bound 3 and `limit=1`, the public graph stopped after observing the second admitted Relation, returned `candidate_relations_scanned=2`, but claimed `candidate_scan_truncated=true` and emitted a hard-coded 5,000-row Gap | Candidate-scan truncation was derived from the existence of an extra candidate before knowing why scanning stopped; the Gap display ignored the active bound | Python graph and its CLI/MCP/Wiki projections; selection truncation could be misreported as an uninspected candidate tail, making the receipt internally contradictory | Derive candidate-scan truncation only after the actual scan bound is reached with an uninspected tail; keep selection truncation independent; render the active bound in the Gap; keep 500 admitted / 5,000 scanned production limits | Public regressions cover selection early exit, full return, rejected tail and actual scan-bound truncation. Small fixtures do not substitute for Relation 500/5,000/10k/100k qualification |
| P7-MEASURE-001 | The Semantic runner named owner-local Capsule v3 bytes `context_provider_payload_bytes`, used Query Plan v5 as the headline surface, named UTF-8 bytes `total_query_tokens`, and calculated `bytes_saved_ratio` from selected content rather than the complete visible payload | The historical v1 query diagnostic had accumulated Context checks without an outcome-specific contract or explicit delivery boundaries | Qualification measurement; byte/token savings and transport overhead could be materially overstated even when functional cases passed | Add closed development-only Context outcome/cost v2 contracts. Make Query Plan v6 `knowledge context` the primary outcome; retain v5 `knowledge query` as diagnostic. Measure local Capsule, Provider Capsule, complete MCP tool result, selected content and transport metadata separately. A byte-derived token value is named an estimate; actual tokens stay null; token savings and distractor delta stay `not_executed` without frozen comparable runs | The v2 report is `tuning_used_development`, `qualification_eligible=false`, and validates exact Provider/MCP bounds. Historical v1 remains readable; its savings denominator now uses its complete v5 payload. Human Gold, actual Provider tokens and equal-budget outcome comparison remain `not_executed` |
| P7-GOLD-001 | The first complete post-fix suite rejected the repository-visible development v3 source binding for `src/deeplaw/knowledge_autonomy.py` | The claim-ineligible current-source fixture still contained the pre-Pass-7 byte hash after the accepted graph correctness change | Local development protocol integrity; retaining a stale hash rejects the intended source, while changing labels/answers/thresholds would contaminate the fixture | Rotate only that one current-source SHA-256. Preserve historical v1, cases, labels, expected IDs, thresholds and governance fields byte-for-byte | Repository development/evaluation/reproducible-build checks are rerun. This repository-visible fixture remains claim-ineligible and is not external Gold |
| P7-EXTERNAL-001 | No repository-external continuity Human Gold, final-blind replacement policy, Wiki human-task Gold, exact signed/verified Legal Pack + legal Gold, separated compiler/evaluator execution environment, or owner-only Codex evaluation identity was supplied | These are Owner-controlled independence, legality, isolation and credential boundaries; repository fixtures or model output cannot create them | Continuity, shared Wiki, protected evidence and real Codex qualification | Keep the package prepared and stop before external outcome execution or any model call | `prepared_not_executed`; no repository `.env`, prior DeepSeek key, `~/.codex/auth.json`, Desktop login state or Provider was used |

All Luna candidates were reviewed as complete diffs by Sol, and their risk-relevant public tests
were independently rerun before integration. No row authorizes a version bump, tag, signature,
Provider call, RC, GA, registry publication, Platform inventory rotation or competitive claim.
Final release disposition remains `source_candidate_remains_not_released`.
