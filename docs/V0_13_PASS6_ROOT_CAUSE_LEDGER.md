# DeepLaw v0.13 Pass 6 root-cause ledger

Status: current-fix and qualification-preparation evidence only; not a release, Human Gold result,
real-Host result, or independent product qualification.

Baseline: branch `codex/v013-evidence-provenance`, start commit/tree
`abed9d78ddfb80e63cd0ec4a7893db74d7b6191d` /
`0df78b2d02cd78c1aeaba5fd1952eb21c20214df`. Package version remains `0.12.0`.
The accepted implementation commit/tree and current-head CI are recorded in the Pass 6 handoff
after the documentation commit; this file does not self-certify its own future Git identity.

| ID | Reproduction | Root cause | Public surface and risk | Minimum repair | Regression / requalification status |
|---|---|---|---|---|---|
| P6-QUERY-001 | Through Source review → Semantic Compilation → Python/CLI/MCP Context, `Please summarize Policy Alpha requirements` returned the three required Alpha Statements while the Title Case equivalent returned zero and locally rejected ten candidates as `identity_anchor_mismatch` | Query v6 inferred identity anchors from natural-language capitalization, then reused the inference as a hard Statement-admission filter | Default Query Plan v6 Context; formatting alone could create false suppression and an incorrect Gap without any Authority change | Keep explicit `semantic_key` / `knowledge_id` / `revision_id` constraints strict, but treat natural-language identity anchors as bounded discovery/rerank/selection relevance hints; do not add entity dictionaries, change Authority, or widen item/character/token/source/provider limits | Public metamorphic development suite covers lower/sentence/title case, punctuation, quotes, multiple targets, CJK + English, exception/negation, ambiguous alias, opaque unknown identity, and explicit targets. Python/CLI/MCP preserve the same required stable IDs and 64 KiB bound. This is tuning-used development evidence, not external qualification |
| P6-QUERY-002 | A read-only audit of v5 compatibility did not reproduce an inferred-anchor bypass of its deterministic relevance floor through a stable public result | The hypothesis was not established independently of private scoring internals | Explicit Query Plan v5 compatibility | No v5 runtime change; record `not_reproduced` instead of changing visible-v5 rules to fit Pass 6 | Existing repository-visible v5 Semantic development suite is rerun separately from v6; neither is external Gold |
| P6-GRAPH-001 | A public canonical graph with three governance-admitted Relation Revisions and `limit=1` returned one Relation but exposed neither selection truncation nor a Gap; a full limit and a governance-rejected tail could not be distinguished from that response | The graph budget reported the selected count and candidate scan bound, but did not probe for the first additional admitted Relation after reaching the result limit | Python graph, CLI/MCP graph and Wiki local graph; a caller could mistake an incomplete relation view for a complete one | Within the existing 5,000 scan bound, continue only until the first extra admitted Relation; report independent `selection_truncated` and `candidate_scan_truncated`, actual scanned/selected counts, and at most two bounded gaps; keep 500 admitted / 5,000 scanned hard limits | Public domain, CLI, MCP and Wiki regressions cover limit=1, complete, governance-rejected-only and candidate-scan-truncated cases. The 5,001-Statement lane passed, but the attempted public semantic `relation_actions` fixture reached v3 finalization and was rejected because its frozen admitted-candidate context exceeded the 64 KiB Provider bound. No private SQL/write fallback was used; Relation 500/5,000 remains `qualification_fixture_blocked` / `not_executed` |
| P6-PACKAGE-001 | `owner_bound_external` accepted duplicate mount IDs, could use an implicit default root, could resolve compiler-only and evaluator-only domains to the same directory, and did not close artifact-kind/purpose/visibility placement | The v1 package closed file hashes and references but treated mount descriptions as caller narrative instead of validating the isolation topology it claimed to record | Owner outcome preparation; Gold/scorer/output bytes could be placed on a compiler-visible or shared root and still look structurally bound | Require unique mount IDs and an exact roots mapping for every external mount; forbid external `package_workspace`; close purpose/visibility/artifact placement; require distinct resolved compiler-only/evaluator-only roots; retain byte/reference-only semantics and explicitly disclaim OS-sandbox proof | Negative tests cover duplicates, missing/extra mappings, shared roots and protected/corpus placement. Credential-free synthetic dry-run remains `prepared_not_executed`, `benchmark_only=true`, `claim_eligible=false`, `assembly_enabled=false` |
| P6-PLATFORM-001 | Current common collection differs from the immutable 1,339-case Platform Core inventory | New current-fix tests are intentionally outside the historical frozen candidate inventory | Platform qualification; current-source CI must not be confused with the nine-cell exact-inventory Core Gate | Do not rotate the inventory before independent product outcomes and final candidate freeze; run manual preflight and preserve its non-zero fail-closed result | Current collection count/digest and the failed manual preflight are recorded in the Pass 6 disposition; Platform Core remains `not_executed` |
| P6-GOLD-001 | The repository-visible development v3 fixture still bound the pre-Pass-6 bytes of `knowledge_autonomy.py` and the synchronized current `ARCHITECTURE.md` contract | A current-source development byte binding does not rotate itself when accepted runtime/documentation bytes change | Development protocol integrity; leaving stale hashes would reject the intended current source, while changing cases/answers/thresholds would contaminate the fixture | Rotate only those two current development v3 source SHA-256 values; keep historical v1, cases, labels, expected IDs, thresholds and governance fields unchanged | Repository development integrity tests are rerun; this visible fixture remains claim-ineligible and is not external Gold |
| P6-EXTERNAL-001 | No repository-external independent Human Gold/final-blind pair, exact verified Legal Pack, isolated Codex evaluation identity, or independent evaluator/scorer workspace was supplied to this task | Required Owner-controlled inputs are external trust and independence boundaries, not repository fixtures an implementation Agent may fabricate | Continuity, Human/Agent Wiki, protected/legal evidence and real Host claims | Prepare the closed owner package and stop before model calls or independent claims | Outcome status is `prepared_not_executed`; model output is not Gold and no credential or Provider was used |

No row authorizes a version bump, tag, signature, Provider call, RC, GA, registry publication, or
competitive claim. The release disposition remains `source_candidate_remains_not_released`.
