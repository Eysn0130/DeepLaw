# DeepLaw v0.13 Pass 5 root-cause ledger

Status: current-fix and qualification-infrastructure evidence only; not a release or an
independent qualification result.

Baseline: branch `codex/v013-evidence-provenance`, start commit/tree
`b83016d92e2e74a34b753330f2f1e8a0452e8a74` /
`e727b5f01526633c721477b5fb36b01cad9bab57`. Accepted implementation commit/tree:
`7a9d7d1f242fe46db1f258737305369f50ec69de` /
`76aae2d9a42622db2984c86f3b3fa667f21c8f5d`. Package version remains `0.12.0`.

| ID | Reproduction | Root cause | Public surface and risk | Minimum repair | Regression / requalification status |
|---|---|---|---|---|---|
| P5-PLATFORM-001 | Frozen common inventory contained 1,339 tests while the initial current collection contained 1,341; after Pass 5 tests it contains 1,370 | Candidate CI collected current source without an exact frozen-inventory preflight, so green current-source regression could be confused with Platform Core | CI/release workflow; stale inventory could create a false portability/release statement | Add a closed collection receipt with sorted IDs, count, digest, selection, HEAD/tree and dirty state; candidate mode records drift, manual Platform Core writes a receipt and exits non-zero on dirty state or drift; the historical manifest is unchanged | Candidate receipt executed and remains `release_ready=false`; manual Platform Core deliberately fails closed; final inventory rotation and nine-cell Platform Core are `not_executed` |
| P5-DOC-001 | `PRD_1_2_RELEASE_DISPOSITION.md` bytes hashed to `003a3361…89e4` while its sidecar contained a different digest | The document changed without rotating its byte sidecar | Evidence integrity; a stale sidecar could bind a report to the wrong disposition | Correct the sidecar and add positive plus changed-byte/wrong-name/wrong-digest tests | Local regression passed; this does not qualify any product capability |
| P5-DOC-002 | Pass 4 limitations still said the Windows zero-Statement cause was not identified | The historical limitation text was not reconciled with the later bounded runtime diagnosis | Audit accuracy; a stale narrative hid a known closed-environment defect | Add a current-head factual correction while preserving the historical raw report | Documentation-only correction; native Platform Core remains pending |
| P5-PROV-001 | A v1 Gate Result could contain unresolved `input_refs`, `frozen=false`, caller-written run/dimension summaries, non-finite metrics, and validator identity without source/executable bytes | The candidate envelope described provenance fields but had no local reference/digest/derivation closure | Commercial evidence; fabricated observations could look structurally valid | Keep candidate-only v1 because no consumer is enabled; add stable input IDs, closed refs, `const: true`, finite checks, derived run/dimension checks, validator file bindings, 64 MiB bounds, and byte/record/result revalidation | Negative tests pass. This generic validator does not run a scorer or establish pass. Twelve Core semantic validators remain incomplete and assembly stays disabled |
| P5-PACKAGE-001 | There was no owner-executable manifest connecting the exact candidate, three data layers, isolation receipts, raw outputs, scorers, validators and three product outcomes | Existing pieces had no bounded lifecycle/reference-closure package | Gold isolation and release scope; self-authored inputs could be mistaken for independent evidence | Add benchmark-only product-outcome package v1 and Owner protocol; reopen every file, close refs/mounts/lifecycle, bind exact product Gate IDs, and reject every `passed` result in v1 | Credential-free dry run passes only as `prepared_not_executed`; external Human Gold, Legal Pack and Host evidence remain absent |
| P5-WIKI-001 | Two public `upsert_concept` objects sharing an admitted alias were canonical but the derived Registry/Resolver could not see the aliases | Validated aliases lived in canonical revision metadata but `_current_rows` omitted them from the page-record seam | Wiki identity/navigation; ambiguity could be hidden and users could be routed by titles instead of governed identity | Promote only the admitted canonical metadata alias list into projection rows; do not read editable frontmatter/path text | Public sink → rebuild → Registry/Resolver test returns explicit two-candidate ambiguity; link indexing remains fail closed for freshness outside its admitted policy |
| P5-WIKI-002 | Wrong-merge, alias-collision and cycle tests were skipped as fixture limitations | Existing public mutation/graph seams already supported the fixtures | Qualification completeness; skipped public behavior was being reported as unavailable | Replace the three skips with public `knowledge_sink`/domain-service tests; preserve stable IDs, typed Relation Revisions, bounded graph output and user-owned Markdown | All three development tests execute. They are not independent human Wiki tasks and do not make the Wiki capability Qualified |
| P5-QUERY-001 | CJK semantic cases 10/11/14 showed expansion replacing the source query with generic English aliases; a fresh development challenge also exposed target-name/distractor ambiguity | Discovery text treated expansion as replacement, and ordinary Statement admission had no bounded auditable proper-name anchor | Context correctness; wrong target, false suppression, stale evidence or unrelated primary evidence could reach the Provider | Keep normalized source query first and expansion additive; bind expansion profile/lexicon/configuration digests; add bounded entity-lexicon-free identity anchors over canonical title/key/aliases/text, without changing Authority or budgets | Canonical semantic suite and tuning-used challenge pass locally. The challenge has been consumed during remediation and is development material, not Human Gold or holdout |
| P5-SCALE-001 | The old 5,001-Statement fixture wrote semantic tables/private publication state directly | The fixture optimized setup instead of exercising Source Revision → semantic compilation → Ledger commit | Evidence validity; a passing result could bypass production invariants | Use the public v3 `begin/next_packet/stage_observations/inventory/finalization/stage_publication/validate/commit` chain with 250-Statement Source shards and digest-bound Source/Compilation/Knowledge Revision inventories | 5,001 Statements execute with first/middle/tail recall and bounded Provider output; 10k/100k remain `not_executed` |
| P5-GRAPH-001 | A public graph call with `limit=1` reaches its selection bound but exposes no selection-truncated flag, Gap or Receipt | The graph budget reports selected count, but not whether more admitted results were omitted after selection | Evidence completeness; callers cannot distinguish a complete graph from a selection-truncated graph | No runtime change in Pass 5 because product outcome qualification has not passed; record a correctness blocker rather than remove/increase bounds | 101-node diagnostic covers tail/hub/deep/cycle/contradiction/temporal/dangling and hops 0/1/2; 500/5000/10k/100k truncation lanes remain blocked/not executed |
| P5-BUILD-001 | The first full local run ended `1 failed, 1371 passed, 6 skipped` because the reproducible-build test saw new untracked contract files in the worktree but its clean tracked materialization could not package them | Reproducible build intentionally uses the Git index, while the new contracts had not yet been staged | Supply-chain regression, not a runtime defect; leaving it unresolved would ship incomplete contracts | Stage the intended contract files, then rerun the exact failed reproducible-build test | Exact failed test passed after staging; the final complete suite is recorded in the Pass 5 disposition |

No row authorizes a version bump, tag, signing, provider call or release. The correct release
disposition remains `source_candidate_remains_not_released`.
