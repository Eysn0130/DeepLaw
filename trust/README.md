# Official catalog trust roots

This directory contains public verification keys only. DeepLaw packages these
keys as the out-of-band trust root for detached official-catalog signatures.
Private signing keys must never be stored in this repository.
