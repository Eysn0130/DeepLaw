# DeepLaw 2.0 改造施工方案

状态：已按 `v0.4.0` 完成第三次发布级复核与当前已知 P0/P1 闭环，2026-07-26。外部
held-out 证明仍为未满足的研究/宣称门禁，不能据此宣称跨系统领先。

## 一、最终定位

DeepLaw 2.0 是面向下一代 AI Agent 的本地优先知识操作系统。它把人类信息、项目经验、工具
结果和领域资料编译为可验证、可演化、可共享的 Knowledge Assets，为 Codex、Claude Code、
OpenCode 等 Agent 提供长期知识、上下文编排、关系导航和受控成长能力。

DeepLaw 不替代 Agent Runtime、IDE、模型、会话系统或项目数据库。它提供独立知识层：

```text
Agent 负责推理与执行
DeepLaw 负责来源、生命周期、选择、上下文、验证与知识治理
```

中国法律能力继续作为首个严格领域包存在。仓库名、Python 包名和 CLI 保持 `DeepLaw` /
`deeplaw`；产品统一称为 DeepLaw 2.0，软件版本独立使用语义版本号。

## 二、对原施工方案的复核结论

原方案判断正确的部分：

1. Knowledge Compiler、Context Compiler、Knowledge Verification 应是核心；
2. Agent 不应每次重新读取全部资料；
3. 知识需要区分临时状态、项目决策、经验、智慧与领域资料；
4. 人类需要 Markdown/Obsidian 视图、反向链接和明确的反馈流程；
5. 图、语义检索和生成式解释只能帮助发现，不能替代真源；
6. Agent 间应传递小型、有预算、有来源的 Knowledge Capsule。

原方案中必须修正的部分：

| 原方向 | 隐患 | 最终决策 |
| --- | --- | --- |
| Phase 0 去法律化 | 会丢失签名目录、版本、时效和不可变 release 边界 | 通用内核与 Legal Pack 并存，互不降级 |
| Knowledge Atom 取代 chunk | 语义单元无法复现原文件，摘要错误会成为第二真源 | 来源 fragment 永久独立；Asset 只是其上层资产 |
| Markdown First 作为真源 | 缺少事务、唯一约束、审计、隔离与可靠生命周期 | SQLite 为规范存储，Markdown 是确定性投影 |
| Agent `remember()` / `learn()` | 容易把幻觉、工具错误和存储型 prompt injection 永久化 | MCP 完全只读；CLI 只能生成 proposal，人工批准 |
| 自动总结进入长期记忆 | 产生 context collapse、旧结论污染和不可逆偏差 | 反馈永不自我提升，替代/撤销均显式 |
| 数字 confidence 决定可信度 | 未校准分数会伪装成事实或审核 | 使用离散 verification/trust/status，不虚构置信概率 |
| 导入包继承原状态 | 可通过第三方包洗白 trust 和审核状态 | 所有导入均为 `untrusted + quarantined` |
| 单一 MCP 混合全部能力 | 普通任务被带偏，工具 schema 和检索结果污染上下文 | Legal Pack 与 Knowledge OS 使用独立显式插件 |
| 自动纳入案件资料 | 造成跨案件泄漏并破坏 Analytix 数据边界 | Analytix 案件项目永远在 DeepLaw 范围之外 |

## 三、最终架构

```text
                              Agent Host
                  Codex / Claude Code / OpenCode
                               |
                   explicit Skill + read-only MCP
                         /                 \
          knowledge_support                law_support
                  |                            |
      General Knowledge Asset Core        Chinese Legal Pack
                  |                            |
     owner-only mutable vault           immutable signed releases
     human-reviewed lifecycle           official/private law scopes
                  |                            |
       Knowledge Capsule                    Evidence Pack

       Analytix case projects / case chats / case databases stay outside.
```

两条路径只共享软件包和安全原则，不共享规范数据：

- Knowledge Asset vault：面向非案件项目知识、决策、约束、经验、工具结果和领域资料；
- Legal Pack：面向公共法律和用户私有法律参考资料；
- Analytix case project：面向单案件附件、会话、事实、SQLite/DuckDB，由 Analytix 自己管理。

### 权威、更新与 Agent 修改边界

| 范围 | 可更新者与更新方式 | DeepLaw Agent 接口永久禁止 | 权威含义 |
| --- | --- | --- | --- |
| 通用 Knowledge Asset vault | 本机 owner 通过 CLI 提议、人工审核、显式替代/撤销 | DeepLaw Agent 接口不得写入、审核、导入、删除或绕过 quarantine；restricted 不可读取 | 固定 `legal_authority=false`；`human_verified` 只表示人工审核激活 |
| 官方 Legal Pack | DeepLaw 团队发布递增、签名目录；用户显式 install/update/disable/uninstall | DeepLaw Agent 接口不得修改法条正文、版本、时效、来源、segment、关系、审核状态或 ACTIVE | 固定 release 内可复核的官方研究来源；修正只能发布新 release |
| 用户私有法律资料 | 当前 OS 用户通过 `private add/delete` 重建独立快照 | 不得改写官方目录或继承官方 authority/rank/review；MCP 不得上传/删除 | 用户提供、未经官方审核的法律参考 |
| Analytix 案件项目 | 仅 Analytix 管理每案件附件、会话、SQLite/DuckDB | DeepLaw 不读取、不摄取、不索引、不共享 | 案件事实与证据，不是 DeepLaw 知识 scope |

普通用户不能在通用 vault 自行声明 `verified_source`；该枚举只为未来独立的发布者验签通道
保留。把法条放入通用 vault 也不会提升其权威，法源研究必须使用 `law_support`。

“Agent 接口只读”不等于能防御宿主另行授予的同账户任意 Shell/文件写权限。Codex、Claude Code
或 OpenCode 接入必须通过宿主工具策略禁止 Agent 写 `~/.deeplaw` 和调用离线管理命令，或者让
MCP 使用独立 OS 身份运行；否则 DeepLaw 无法把同一 owner 的进程可靠区分为“人”或“Agent”。

## 四、Knowledge Asset 规范

Asset 类型：

```text
constraint, decision, fact, procedure, rule,
experience, lesson, question, reference
```

记忆层级：

```text
working, project, experience, wisdom, domain
```

生命周期：

```text
proposed ──human review──> active
    │                        │
    └──risk──> quarantined   ├──explicit replacement──> superseded
                             └──explicit withdrawal───> revoked
```

关键不变量：

1. `active` 必须是 `human_verified`；
2. working memory 必须有 `expires_at`；
3. 相同 `semantic_key` 同时最多一个 active；
4. 替代必须明确记录 `supersedes_asset_id`；
5. `trust` 只描述来源，不表示真实；
6. source-bound Asset 必须保留 fragment、locator 和 quote hash；
7. restricted Asset 不进入 Agent MCP；
8. 失效、撤销、隔离和待复核 Asset 不进入 Context Capsule；
9. event chain 必须与当前 Asset/source/fragment/relation/FTS 状态完全重放一致；
10. source-bound Asset 在实际返回、Context 或导出前必须重新验证所选原件；
11. quarantine 激活需要普通人工审核之外的第二个风险确认；
12. 通用 Asset 固定 `legal_authority=false`，不能替代 Legal Pack。
13. 法律单主题导航不得展开入站关系图；一跳关系只服务明确的版本、例外、替代或反证问题。
14. 未提供 `as_of` 时，已知历史、废止、替代或尚未施行的法源只能进入
    `uncertain_evidence`，不得覆盖主证据义务。

## 五、Knowledge Compiler

已实现输入：

- PDF、DOCX、通过本机 LibreOffice 受控转换的 legacy DOC；
- UTF-8 TXT、Markdown、JSON/JSONL；
- 常用源代码、SQL、CSV/TSV、YAML、TOML、XML、HTML、CSS 和日志。

通用 UTF-8 提取保留行结构、空行和内部缩进，并在 fragment 生成前执行文件、字符、单行和
block 数量门禁。

编译链：

```text
source bytes
  → content identity
  → bounded fragments + stable locators
  → source-bound Asset proposals
```

安全策略：

- PDF 原生文本质量不足时失败关闭，必须显式启用文档引擎或提供人工复核结果；
- 文档模型只由独立 CLI 管理动作获取；固定 repository revision 的 15 个文件必须逐项通过
  文件集合、大小与 SHA-256，摄取时清除上游配置覆盖并强制 local-only，禁止自动下载；
- 指令式文本、不可见字符和双向控制字符触发 source quarantine；
- 手工 proposal 使用同一风险检测，quarantine 不能仅凭普通审核确认激活；
- 同一字节内容在不同 trust、sensitivity、origin 或 title 下不能混淆身份；
- 每次 ingest/propose/debug/feedback 都要求确认不是 Analytix 案件资料；
- Context Capsule 会持久化 task/goal，因此 CLI 与 MCP context 也必须显式确认非案件数据；
- 处理成功从不等于审核通过。

## 六、Context Compiler 与 Knowledge Capsule

Context Compiler 只搜索 active、human-reviewed、未过期的 Asset：

```text
task + goal
  → bounded lexical discovery
  → one bounded human-reviewed relation hop
  → constraint / decision / rule / procedure priority
  → item + character budget
  → reviewed bounded relations
  → evidence + gaps
  → Knowledge Capsule
```

选择器先验证候选，再按剩余条目进行公平字符分配；长 Asset 不能独占全部预算。正文 excerpt
围绕实际查询命中位置产生，同一 compiler section 的多个 part 先去拥挤；低于最佳词法候选相对
阈值的尾部候选失败关闭。所有截断 excerpt 仍必须能从当前 Asset statement 精确重放。

Capsule 包含：

- task、goal；
- constraints、decisions、knowledge assets、experiences、open questions；
- source references 和 reviewed relations；
- 最大条目、最大字符、实际使用和预算排除数；
- 来源引用数量/字符预算和完整序列化载荷上限；
- 每项 lexical/reviewed-relation 选择原因；
- gaps 和 next actions；
- vault revision、历史 audit head、digest 与 capsule ID；
- 明确的 prompt-injection trust boundary；
- 固定 `legal_authority=false`、`case_data_allowed=false` 和 `law_support` 权威路由。

验证器检查：

1. 封闭 schema；
2. 全字段摘要和 Capsule ID；
3. 历史 audit anchor；
4. 当前 Asset hash、URI、状态、审核和过期状态；
5. vault revision 变化并单独报告 `stale`；
6. question 内容不会直接成为命令；`next_actions` 只包含固定审阅动作与 Asset URI。

## 七、长期知识与受控成长

DeepLaw 不保存完整会话。宿主保留 session；DeepLaw 只保存经判断值得跨任务复用的知识：

| 层级 | 保存内容 | 退出方式 |
| --- | --- | --- |
| working | 临时任务状态 | 自动过期 |
| project | 当前项目决策、约束、事实、流程 | 替代或撤销 |
| experience | 失败、修复、复盘与 Capsule 反馈 | 人工复核后激活 |
| wisdom | 多项目稳定模式 | 人工提升 |
| domain | 领域参考和规则 | 独立治理 |

Knowledge Debugger 和 Capsule Feedback 已实现，但只生成 proposal；Feedback 必须提交与
当前 vault 验证绑定的真实 Capsule 文件，不能只提供一个形似合法的 ID。Agent 永远没有
`remember`、`learn`、`approve`、`import` 或 delete MCP operation。

## 八、关系、图和人类视图

v1 仅允许小型、人工复核关系：

```text
supports, contradicts, depends_on, implements,
derived_from, applies_to, related_to
```

关系不允许 self-loop，Agent 只能在已选 Asset 周围做有界读取。已选知识之间的
`contradicts` 会成为显式 gap，系统不会自行选择一方。模型生成图、自动本体和多跳推理不是
权威路径。

Markdown/Obsidian 导出已实现：

- 固定路径由 memory tier、kind 和 asset ID 决定；
- 包含 front matter、来源和双向关系；
- 生成文件有 hash manifest；
- 只有封闭 manifest、全部文件 hash 一致且无额外用户文件的目录才允许 `--replace`；
- SQLite vault 始终是规范真源。

## 九、可迁移 Knowledge Package

`.dlk` v1 已实现：

- 固定 vault revision 可复现；
- 按 sensitivity 上限导出 active、未过期 Asset；
- 可选择带 source fragment 和 source file；
- manifest 的身份字段和所有 payload 都进入 SHA-256；
- 重新计算 Asset、source、fragment 与 relation 身份，不能只信自洽 ZIP hash；
- 防 path traversal、重复条目、超大条目、压缩炸弹和记录数失控；
- relation evidence 超过导出敏感级别时失败关闭；
- 导入不继承远端 trust/status，一律进入 quarantine。

当前 `.dlk` 只证明内容完整，尚不证明发布者身份。后续独立建设签名、撤销和单调更新通道；在
此之前不能把合法 hash 描述为可信发布者。

## 十、Agent 接入

Legal Pack 插件：

```text
plugins/deeplaw
deeplaw mcp --stdio
law_support
```

Knowledge OS 插件：

```text
plugins/deeplaw-knowledge-os
deeplaw knowledge mcp --stdio
knowledge_support
```

`knowledge_support` 只有五个 read-only operation：

```text
search, get, context, verify, inspect
```

Codex 使用 explicit-only Skill；Claude Code 使用独立插件；OpenCode 默认 deny，由
`@deeplaw-knowledge` 专用 agent 只放行对应 Skill 和单一工具。

## 十一、已经完成的施工

- [x] 通用 Knowledge Asset 数据契约和 SQLite vault；
- [x] owner-only 权限、物理隔离和 symlink 失败关闭；
- [x] source fragment 与 Asset 分层；
- [x] proposal/quarantine/approval/supersession/revocation/expiry；
- [x] 小型人工复核关系；
- [x] Knowledge Compiler 和 PDF 质量门禁；
- [x] Context Compiler 和 Knowledge Capsule；
- [x] Capsule/Asset/audit verification；
- [x] event chain 与当前 Asset/source/fragment/relation/FTS 全状态重放核对；
- [x] 数据库替换、status 直改、FTS 伪造和源文件缺失的 Agent 失败关闭；
- [x] 基于 inode/size/mtime/ctime/revision/audit head 的安全校验缓存；
- [x] quarantine 双重确认、`verified_source` 用户自封阻断与手工 prompt-injection 隔离；
- [x] `legal_authority=false` 机器边界与 Legal Pack 权威路由；
- [x] 一次有界人工关系扩展、选择原因和 URI-only next action；
- [x] Context task/goal 的非案件数据显式确认与 CLI/MCP schema 门禁；
- [x] Capsule Feedback 真文件/当前 vault 绑定验证；
- [x] Knowledge Debugger 与 feedback proposal；
- [x] 确定性 Markdown/Obsidian 投影；
- [x] 可复现 `.dlk`、完整性校验和不可信导入；
- [x] 独立 read-only MCP；
- [x] Agent 只读接口与宿主同账户 Shell 权限分界；Skill 禁止绕过，接入文档要求宿主路径/工具隔离；
- [x] 五种 MCP operation 的封闭判别式 output schema；
- [x] 未配置 vault 时 MCP 仍可完成工具发现，读取返回无路径泄漏的失败关闭错误；
- [x] 真实 CLI 全生命周期验收（ingest/review/context/feedback/supersede/revoke/export/import）；
- [x] Codex、Claude Code、OpenCode 适配；
- [x] Legal Pack 完整保留且测试继续通过；
- [x] 结构化文档引擎 v2 源文本字段闭集适配，扫描正文不再被错误丢弃；
- [x] 文档模型独立显式安装、固定 revision/清单、owner-only 配置、摄取前全量复核与
  local-only 环境隔离；任意额外、缺失、篡改或不安全权限文件失败关闭；
- [x] DOCX 主文与脚注 DTD/实体展开失败关闭，抽取器身份升级为 `deeplaw-ooxml/v2`；
- [x] 15 个风险页 AI 视觉复核与 8 个风险 segment 精确隔离；
- [x] 已知历史、废止、替代和未施行法源在缺少 `as_of` 时禁止进入主证据；
- [x] v0.4 时效分桶评测夹具独立版本化，历史 v0.3 夹具 hash 保持不变；
- [x] Legal Pack derivation 绑定 review governance、collection scope 和完整 build-report
  身份；逻辑 release 与 SQLite artifact hash 同时固定；
- [x] 外部六套件协议、逐题 scorer、配对 bootstrap、Holm–Bonferroni、suite evidence
  manifest、Ed25519 独立 attestation 和拒绝无边界声明的 claim gate；
- [x] LongMemEval-V2 真实 vault 适配器与明确 `claim_eligible=false` 的 60 题开发诊断；
- [x] README、架构、安全和操作文档形成一致闭环。

## 十二、发布门禁

每次变更至少执行：

```bash
uv lock --check
uv run ruff check .
uv run pytest
uv build
git diff --check
```

插件发布另验证：

- plugin manifest 和 Skill schema；
- `tools/list` 只有一个预期 leaf；
- restricted/inactive/source path 不泄漏；
- write/unknown operation 被拒绝；
- 普通任务不隐式激活；
- Legal Pack 和 Knowledge OS 任一插件可以独立安装和删除。

### 2026-07-26 发布证据快照

本次复核在当前工作树实际执行，而不是沿用旧报告：

| 门禁 | 结果 |
| --- | --- |
| 全仓测试 | `350 passed` |
| JSON Schema | 25 份 Draft 2020-12 schema 结构校验通过 |
| 静态与依赖 | ruff、`uv lock --check` 通过；默认运行时 `pip-audit` 无发现；可选文档引擎固定 15 文件/1,082,446,509 bytes 模型清单、离线执行面与 4 个唯一 PYSEC 标识经 OpenVEX 和负向测试复核，新的或未匹配漏洞仍会失败关闭 |
| 构建产物 | sdist/wheel 构建通过；未包含 vault、私钥或法源 PDF/DOCX |
| 安装后 CLI | 用户级 wheel 安装完成完整生命周期、固定模型清单配置、恶意上游环境覆盖负例、真实 PDF、28 份原件重建、私有库隔离与返回预算验收 |
| 攻击性 CLI | 自封 `verified_source`、伪造 Capsule、SQLite 生命周期篡改、来源原件删除均失败关闭 |
| MCP | 两个进程各只有一个只读 leaf；unknown/write operation 与开放参数被拒绝 |
| Codex | 当前 CLI `0.146.0-alpha.3.1` 可用；隔离 `CODEX_HOME` 安装、独立卸载与 MCP 注册沿用 2026-07-25 实测证据 |
| Claude Code | 当前验证环境未安装 CLI；两插件 manifest/Skill/MCP 契约由全仓测试验证，不把静态验证写成宿主实机通过 |
| OpenCode | 当前验证环境未安装 CLI；两份独立配置、默认 deny 和单 MCP 权限由全仓测试验证，不把静态验证写成宿主实机通过 |
| 官方 Legal Pack | 隔离 home 从 28 份原件和固定模型黑盒重建；签名目录 sequence 2 验证通过；release `lawrel_5fd93dee88cd55c68706cd1a3dfa527a`；28 文档、3237 segment、111 relation；数据库 SHA-256 `c5582f5f1553cbcaa4df4a263a16be524307051b7bfa6ded50053dbe3b4287db` |
| 当前 Legal Pack smoke | 更新时效分桶契约后 37/37 通过；27 个主证据目标、10 个不确定证据目标；37/37 receipt 有效；25 个用例仍显式保留 83 个 blocking gap |
| PDF 风险页 | 15 页完成 AI 视觉复核；8 个风险 segment 失败关闭，未伪造 human attestation |
| Context 开发诊断 | LongMemEval-S cleaned 60 题；Capsule Recall@5 `0.6283 → 0.8906`，重复归零，正文约 `5000 → 3647` 字符；已用于开发，固定为 `claim_eligible=false` |
| 外部宣称门禁 | 六套件协议 canonical hash 冻结；synthetic 完整 pass-path 与篡改/pending 负例通过；真实 evidence 仍为 `pending_external_execution`，无边界声明退出码 2 |

该快照证明当前实现和发布契约通过门禁，不证明跨系统知识质量领先；后者仍受下一节 held-out
研究门禁约束。

## 十三、下一阶段，只能按证据推进

这些目标仍是 P2/研究门禁，不能在主页宣称已经具备；它们不构成当前 P0/P1，因为现有路径
分别通过不继承信任、单用户边界、禁用默认 sidecar 和禁止自动学习而失败关闭：

1. `.dlk` 发布者签名、撤销和单调更新；
2. 多租户认证、加密和远程服务；
3. 自动 ontology、生成式 graph 或语义检索能稳定提高任务成功率；
4. 自动长期学习安全且不会产生 memory poisoning；
5. 跨知识系统的质量领先。评测协议和机器门禁已经完成，但六套件真实运行、秘密 held-out 和
   两家独立复现尚未取得。

后续研究必须先建立 held-out benchmark，统一比较：

- task success；
- useful-context recall；
- irrelevant-context rate；
- provenance coverage；
- stale/contradiction detection；
- memory-poisoning success rate；
- Capsule characters/tokens；
- latency、memory 和 cost。

只有某个 sidecar 在相同语料、任务和预算下显著改善结果，且不破坏来源、审核、隔离和上下文
门禁，才允许进入默认路径。否则保持可选、派生、可删除。

“全面超过所有知识库”在逻辑上无法覆盖未公开、未来出现或未参评系统，因而永久不能成为
可验证主张。最高等级文案只能由
[`benchmarks/external/claim_gate.py`](benchmarks/external/claim_gate.py) 在完整证据到齐后生成
协议限定的比较结论；维护者不能手写替代。

## 十四、最终验收标准

DeepLaw 2.0 的正确性不是“功能最多”，而是同时满足：

```text
来源可回放
生命周期可治理
正文、来源元数据和完整载荷都有硬预算
缺口不隐藏
反馈不自我提升
插件不带偏宿主
领域包不被通用化降级
案件数据不越界
共享不洗白 trust
所有当前能力有测试证据
```

对应实现细节见：

- [`docs/KNOWLEDGE_OS.md`](docs/KNOWLEDGE_OS.md)
- [`docs/KNOWLEDGE_OS_RESEARCH.md`](docs/KNOWLEDGE_OS_RESEARCH.md)
- [`docs/EXTERNAL_BENCHMARK_PROTOCOL.md`](docs/EXTERNAL_BENCHMARK_PROTOCOL.md)
- [`docs/IMPLEMENTATION_AUDIT_2026-07-26.md`](docs/IMPLEMENTATION_AUDIT_2026-07-26.md)
- [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md)
- [`docs/AGENT_ADAPTERS.md`](docs/AGENT_ADAPTERS.md)
- [`SECURITY.md`](SECURITY.md)
